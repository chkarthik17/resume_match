version: '3.9'

services:
  resume-matcher:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATABASE_URL=${DATABASE_URL}
    volumes:
      - backend_uploads:/app/backend/uploads
    restart: unless-stopped

volumes:
  backend_uploads:
